# 应用端口
server:
  port: 7901
spring:
  application:
    # 应用名称
    name: provider0

# eureka 配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://ampei:123456@localhost:8761/eureka

logging:
  level:
    root: INFO



