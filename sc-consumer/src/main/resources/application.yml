server:
  port: 8001
spring:
  application:
    name: consumer

# eureka 配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://ampei:123456@localhost:8761/eureka
logging:
  level:
    root: INFO